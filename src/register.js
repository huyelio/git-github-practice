function register(user, password) {
  if (user == "admin" && password == "123") {
    return true;
  }
  return false; //them exception
}
function deleted(user, password) {
  if (user == "admin" && password == "123") {
    return true;
  }
  return false; //them exception
}
